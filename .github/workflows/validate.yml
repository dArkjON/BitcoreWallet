name: Validate Bitcore Wallet

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_dispatch:

jobs:
  validate-bitcore:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Quick Validation
      run: |
        echo "🔍 Quick Bitcore Wallet validation..."

        # Only check if essential files exist - no complex validation
        if [ -f "blue_modules/bitcore-network.ts" ] &&
           [ -f "blue_modules/BlueElectrum.ts" ] &&
           [ -d "ios/BlueWallet.xcodeproj" ] &&
           [ -f ".github/workflows/build-ios.yml" ]; then
          echo "✅ Core files present - ready for iOS build"
        else
          echo "❌ Some core files missing"
          exit 1
        fi

    - name: Ready for Build
      run: |
        echo ""
        echo "🎉 QUICK VALIDATION COMPLETE!"
        echo "📱 Bitcore Wallet ready for iOS Altstore build"
        echo ""
        echo "🚀 Next step: Trigger iOS build workflow"
        echo "   • Bitcore network files present"
        echo "   • iOS project structure ready"
        echo "   • Build workflow configured"
        echo ""
        echo "⚠️ Note: Skipping detailed validation to focus on build testing"